<div class="container">
    <div class="row">
        <div class="col-md-5">
            <div class="panel panel-primary">
                <div class="panel-heading"><%= action %></div>
                <div class="panel-body">

                    <%= form_for @bid do |f| %>
                        <% if @bid.errors.any? %>
                            <div id="error_explanation">
                                <h2>
                                    <%= pluralize(bid.errors.count, "error") %> prohibited this bid from being saved:</h2>
                                <ul>
                                    <% bid.errors.full_messages.each do |message| %>
                                        <li>
                                            <%= message %>
                                        </li>
                                        <% end %>
                                </ul>
                            </div>
                        <% end %>

                        <div class="field">
                            <%= f.label :bid_amount %>
                                <%= f.text_field :bid_amount %>
                        </div>

                        
                        <div class="field">
                            <%= f.label :user_id %>
                                <%= f.text_field :user_id%>
	   
                        </div>
                        
                        <div class="field">
                          <%=f.label:product_id%>
                              <%= f.text_field :product_id%>
                          <p>Kettle
                          <%= image_tag('dummy_product.png', style: "height: 40px") %>
	                        </p>
                        </div>                        

                        <div class="actions">
                            <%= f.submit action, class: "btn btn-success" %>
                        </div>
                    <% end %>
                </div>
            </div>
        </div>
    </div>
</div>
