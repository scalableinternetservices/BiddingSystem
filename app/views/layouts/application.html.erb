<!DOCTYPE html>
<html lang="en">
	<%= render "head" %>
	<body>
		<%= render "menubar" %>
		<div class="container">
			<div class="row">
				<%= render "sidebar" %>
				<%= render "header" %>
				<%= yield %>
			</div>
		</div>
		<%= render "footer" %>
	</body>
</html>